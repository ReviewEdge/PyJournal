# Make sure you set my_project_path to equal your project's path
# When you create your config file, make sure it is moved to the project's directory

# make sure to us r in front of the string so it is interpreted as a raw string (so you don't have issues with slashes)
my_project_path = r"C:\Users\Logan\PycharmProjects\PyJournal"
